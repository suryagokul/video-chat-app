1) Call the express function

2) Create the HTTP Server and pass express app to it.

3) Require Cors. 

4) Require socket.io and pass server to it.

5) Use the Cors.

6) Create the HTTP Get function using express app.

7) Create PORT 

8) Server need to listen for connection on given port.

9) Then Comes our main socket code given below - 

        io.on("connection", (socket) => {
            socket.emit('me', socket.id);

            socket.on('diconnect', ()=> {
                socket.broadcast.emit('callended');
            })

            socket.on('calluser', ({userToCall, signalData, from, name})=>{
                io.to(userToCall).emit('calluser', {signal: signalData, from, name});
            })

            socket.on('answercall', (data)=> {
                io.to(data.to).emit('callaccepted', data.signal);
            })
        })

10) Now we have to work with our frontend part.. In terminal npx create-react-app ./client

11) Install requirements for the frontend --> 

    npm i @material-ui/core @material-ui/icons react-copy-to-clipboard simple-peer socket.io-client

